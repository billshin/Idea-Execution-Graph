# 全局
node & link line 要可以del , 

ctrl + z 可以復原

右側 Node Detail 取消顯示, 改為雙擊node 顯示

右側 Task Parking Lot 先移到左測

# Node Detail
node 點兩下在跳出編輯視窗
node 顯示的文字項目可議直接編輯
node status 點下可以下拉選單改變狀態



## 按鈕修正
統一跟status依樣放在node 右上角
- Node 只顯示 +
- Collapse/Expand 按鈕改成一個按鈕,點一下切換狀態

順序 [Collapse/Expand] | [Status] | [+]

# Workspace Controls

Custom Display 追加顯示 task list