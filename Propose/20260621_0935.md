# 全局

# 左側
新增開關 , edit lock ,lock狀態不接受 node 內編輯行為 

# node 修改

node內文字點一下, 才切換成編輯模式, 直接編輯文字, 按enter或點擊外部, 就會儲存

按鈕修改 , add node 改的跟n8n 依樣右側有個+
![alt text](image/image.png)

node 最下方規劃成 功能區,預設隱藏
status 右側新增一個齒輪,點下去才出現  功能區

[Collapse/Expand] 移動到 功能區

功能區 button 字小一級

